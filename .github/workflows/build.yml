name: Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:

  benchmark:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.26'

    - name: Run benchmarks
      run: |
        go test -bench=. -cpu=8 -benchtime=0.1s -count=6 > bench.txt
        go test -bench=. -cpu=1 -benchtime=0.1s -count=6 >> bench.txt
        go install golang.org/x/perf/cmd/benchstat@latest
        $(go env GOPATH)/bin/benchstat bench.txt > benchstat.txt
        cat benchstat.txt >> $GITHUB_STEP_SUMMARY

    - name: Save PR number
      if: github.event_name == 'pull_request'
      run: echo ${{ github.event.pull_request.number }} > pr-number.txt

    - name: Upload benchmark results
      uses: actions/upload-artifact@v4
      with:
        name: benchstat
        path: |
          benchstat.txt
          pr-number.txt

  benchmark_comment:
    needs: benchmark
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Download benchmark results
      uses: actions/download-artifact@v4
      with:
        name: benchstat

    - name: Comment Commit
      uses: actions/github-script@v7
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          const fs = require('fs');
          const output = fs.readFileSync('benchstat.txt', 'utf8');
          github.rest.repos.createCommitComment({
            commit_sha: context.sha,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: '## Benchmark Results\n```text\n' + output + '\n```'
          })

  build:
    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm]
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.26'

    - name: Build (amd64)
      if: ${{ matrix.os == 'ubuntu-latest' }}
      run: GOARCH=amd64 go build -v ./...

    - name: Build (386)
      if: ${{ matrix.os == 'ubuntu-latest' }}
      run: GOARCH=386 go build -v ./...

    - name: Build (arm64)
      if: ${{ matrix.os == 'ubuntu-latest' }}
      run: GOARCH=arm64 go build -v ./...

    - name: Build (arm)
      if: ${{ matrix.os == 'ubuntu-latest' }}
      run: GOARCH=arm go build -v ./...

    - name: Test
      run: go test -v -race -coverprofile=coverage.txt -covermode=atomic ./...
